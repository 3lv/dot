### Account config
set from = "cristian.vlad.vladutu@gmail.com"
set realname = "vlad"
# IMAP settings
#set imap_user = "kb115055@@gmail.com"
#set imap_pass = "ztmvmmopdnbsozgr"
set imap_user = "cristian.vlad.vladutu@gmail.com"
set imap_pass = "racwwljgimrueamf"
# SMTP settings
set smtp_url = "smtps://cristian.vlad.vladutu@gmail.com@smtp.gmail.com:465/"
set smtp_pass = "racwwljgimrueamf"
# Remote Gmail folders
set folder = "imaps://imap.gmail.com:993/"
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Drafts"
set record = "+[Gmail]/Sent Mail"
set trash = "+[Gmail]/Trash"
set imap_check_subscribed

# General config
set editor = "nvim"
set header_cache = ~/.cache/mutt
# set message_cachedir = ~/.cache/mutt # cost important disk space
# Allow Mutt to open a new IMAP connection automatically.
unset imap_passive
# Keep the IMAP connection alive by polling intermittently (time in seconds).
set imap_keepalive = 300
# How often to check for new mail (time in seconds).
set mail_check = 120
set timeout = 10
 
# Keybinds
bind pager j next-line
bind pager k previous-line
bind attach,index,pager \CD next-page
bind attach,index,pager \CU previous-page
bind pager g top
bind pager G bottom
bind attach,index g first-entry
bind attach,index G last-entry
bind index \CL imap-fetch-mail
